# import struct
import torch
from pandas import DataFrame

# file = './data.bin'

# with open(file, 'wb') as f:
#     for i in range(100, 200):
#         f.write(struct.pack('B', i))
# a = (1, 2, 3)
# print(type(a[:2]))

            
